#include <stdio.h>
#include <stdlib.h>

#include "./Quadtree.h"

int main(int argc, char* argv[]) {
    setbuf(stdout, 0);
    //Quadtree qt = Quadtree_create(1024, Point_create(0, 0));
    //printf("%f %f %f\n", qt.center.x, qt.center.y, qt.length);
    return 0;
}
